#Name: 180517H.Reverse_Method.py

newArray = []

def reverseArray(arr): #todo dequeue then push
    for outLoop in arr:
        for inLoop in arr:
            if arr == arr.empty:
                print("Array reversed")
                return newArray
            elif ( arr != arr.empty ):
                oldItem = arr[0]
                minusLast = arr.split[0]
                newArray.extend(oldItem)


reverseArray("5", "8", "4", "2", "7")

#def thing(a)
#a = [1,2,3,4,5]
#reverse_a = a[::-1]
